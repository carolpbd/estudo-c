// estrutura condicional FOR em C

#include <stdio.h> 
int main() {
    for (int i = 0; i < 5; i++) { //quando começa a ser executada, tem um valor de interaçao, foi definida a variavel que comeaça em 0 e 
        // depois da virgula tem a condiçao de parada, que é i menor que 5, e depois o incremento, que é i++ (i = i + 1)
        printf("Numero %d\n", i);
    }

    return 0;
}